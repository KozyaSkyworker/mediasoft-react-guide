export type TCard = {
    id: number;
    bg: string;
};

export type TCharacter = {
    id: number,
    name: string,
    image: string
}

export type TCounters = {
    hate: string[],
    like: string[],
    idk: string[]
}